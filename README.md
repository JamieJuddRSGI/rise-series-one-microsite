
  # TESTENV
  This is the site for the deployed version of the Private Capital Microsite

  ## Running the code locally
  Run `npm i` to install the dependencies.
  Run `npm run dev` to start the development server.
  